---
'@dotlottie/dotlottie-js': minor
---

add `addImage()`, `getImage()`, `removeImage()` and `extraImages` for packaging images no animation references, e.g. images a theme targets. Also fixes reading an SVG from `i/`.
